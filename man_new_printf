.TH _PRINTF 3 “2017-10-23” “GNU” "Linux Programmer's Manual"
.SH NAME
.LP
_printf - function that prints arguments to stdout

.SH SYNOPSIS
.LP
#include <holberton.h>
.LP
int _printf(const char *format, ...);
.br
int _putchar(char c);
.br
int (*get_func(char ch))(va_list);
.br
int print_char(va_list list);
.br
int print_string(va_list list);
.br
int print_non(char ch1, char ch2);
.br
int print_num(va_list list);

.SH DESCRIPTION
.LP
The _printf() family of functions produces output according to a format as described below.  The _printf() function writes output to stdout, the standard output stream.

.LP
These functions write the output under the control of a format string that specifies how subsequent arguments (or arguments accessed via the variable-length argument facilities of stdarg are converted for output.

.RS -3
.B Return Value
.RE

.LP
This function returns the number of characters printed, excluding the null byte used to end output to strings.

.LP
If an output error is encountered, a negative value is returned.

.RS -3
.B Format of the format string
.RE

.LP
The percent sign represents the format string (‘%’), and is followed by a format specifier.

.RS -3
.B CONVERSION SPECIFIERS
.RE

.LP
Supported format modifiers:
.br
d, i - prints an integer
.br
c - prints a character
.br
s - prints a string
.br
% -  a ‘%’ is written, no argument is converted. The complete conversion specification is ‘%%’.

.SH BUGS
.LP
No bugs as of yet.

.SH SEE ALSO
.LP
print(3)

.SH AUTHORS
.LP
Robert Malmstein, Kevan Lucchini
